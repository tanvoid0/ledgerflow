org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
kotlin.stdlib.default.dependency=false

# Build against the IDE already installed on this machine, so the exact build
# the lessons were verified on is the one we compile with. Override on another
# box: ./gradlew -PidePath="/path/to/IntelliJ IDEA"
idePath=C:/Program Files/JetBrains/IntelliJ IDEA 2026.2.1
